import { currentUrl } from 'oik-html-frontend/helpers/current-url';
import { module, test } from 'qunit';

module('Unit | Helper | current url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentUrl([42]);
  assert.ok(result);
});
