import { isEqualOr } from 'oik-html-frontend/helpers/is-equal-or';
import { module, test } from 'qunit';

module('Unit | Helper | is equal or');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEqualOr([42]);
  assert.ok(result);
});
