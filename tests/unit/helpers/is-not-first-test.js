import { isNotFirst } from 'oik-html-frontend/helpers/is-not-first';
import { module, test } from 'qunit';

module('Unit | Helper | is not first');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNotFirst([42]);
  assert.ok(result);
});
