import { startsWith } from 'oik-html-frontend/helpers/starts-with';
import { module, test } from 'qunit';

module('Unit | Helper | starts with');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = startsWith([42]);
  assert.ok(result);
});
