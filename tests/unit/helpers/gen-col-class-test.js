import { genColClass } from 'oik-html-frontend/helpers/gen-col-class';
import { module, test } from 'qunit';

module('Unit | Helper | gen col class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = genColClass([42]);
  assert.ok(result);
});
