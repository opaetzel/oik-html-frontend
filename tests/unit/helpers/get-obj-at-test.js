
import { getObjAt } from 'oik-html-frontend/helpers/get-obj-at';
import { module, test } from 'qunit';

module('Unit | Helper | get obj at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getObjAt([42]);
  assert.ok(result);
});

