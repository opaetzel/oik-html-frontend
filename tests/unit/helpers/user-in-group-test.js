import { userInGroup } from 'oik-html-frontend/helpers/user-in-group';
import { module, test } from 'qunit';

module('Unit | Helper | user in group');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userInGroup([42]);
  assert.ok(result);
});
