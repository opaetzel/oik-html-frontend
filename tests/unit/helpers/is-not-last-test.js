import { isNotLast } from 'oik-html-frontend/helpers/is-not-last';
import { module, test } from 'qunit';

module('Unit | Helper | is not last');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNotLast([42]);
  assert.ok(result);
});
