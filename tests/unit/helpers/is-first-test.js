import { isFirst } from 'oik-html-frontend/helpers/is-first';
import { module, test } from 'qunit';

module('Unit | Helper | is first');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isFirst([42]);
  assert.ok(result);
});
