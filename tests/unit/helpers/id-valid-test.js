import { idValid } from 'oik-html-frontend/helpers/id-valid';
import { module, test } from 'qunit';

module('Unit | Helper | id valid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = idValid([42]);
  assert.ok(result);
});
