import { isLast } from 'oik-html-frontend/helpers/is-last';
import { module, test } from 'qunit';

module('Unit | Helper | is last');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isLast([42]);
  assert.ok(result);
});
