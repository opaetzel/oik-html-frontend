{{#top-menu}}
    Registrieren
{{/top-menu}}
<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            {{#if sent}}
                <p>Registrierung erfolgreich! Eine Mail wurde an die hinterlegte E-Mail Adresse geschickt, um sie zu bestätigen.</p><br>
                <button {{action "newUser"}} class="btn btn-primary pull-right">Einen weiteren Nutzer registrieren</button>
            {{else}}
                <form {{action 'register' on='submit'}}>
                    <div class="form-group">
                        <label for="identification">Username</label>
                        {{input id='identification' placeholder='Username' value=username class="form-control"}}
                    </div>
                    <div class="form-group">
                        <label for="email">E-Mail Adresse</label>
                        {{input id='email' placeholder='vorname.nachname@uni-goettingen.de' type="email" value=email class="form-control"}}
                    </div>
                    <div class="form-group">
                        <label for="password">Passwort</label>
                        {{input id='password' placeholder='Passwort' type='password' value=password class="form-control"}}
                    </div>
                        <button type="submit" class="btn btn-primary pull-right">Registrieren</button>
                </form>
            {{/if}}
            {{#if error}}
                Ein Fehler ist aufgetreten, bitte versuchen Sie es in wenigen Minuten erneut
            {{/if}}
        </div>
    </div>
</div>
{{outlet}}
